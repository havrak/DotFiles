IMAGE=/tmp/screen_locked.png

# 1. Take a screenshot (Replaces scrot)
grim "$IMAGE"

# 2. Pixelate it (Same logic as before: downscale 10%, upscale 1000%)
# utilizing ImageMagick
magick "$IMAGE" -scale 10% -scale 1000% "$IMAGE"

# 3. Lock the screen (Replaces i3lock)
# -f: Daemonize (detach)
# -i: Image to use
swaylock -f -i "$IMAGE"

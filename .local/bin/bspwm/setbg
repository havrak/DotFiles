#!/bin/sh
# Made by Luke Smith: //github.com/LukeSmithxyz

[ ! -z "$1" ] && cp "$1" $HOME/.local/bin/files/wall.png && notify-send -i "$HOME/.local/bin/files/wall.png" "Wallpaper changed."
xwallpaper --zoom ~/.local/bin/files/wall.png
